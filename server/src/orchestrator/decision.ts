import type { GateDecision } from '@mat/shared';
export function parseDecision(_text: string, _stageId: string, _gateAttempt: number, _validNodeRunIds: readonly string[]): GateDecision { throw new Error('NOT_IMPLEMENTED: orchestrator/decision'); }
