{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the loopback-hosted UI in the main desktop window to open a native folder picker.",
  "windows": ["main"],
  "local": false,
  "remote": {
    "urls": ["http://127.0.0.1:*/*"]
  },
  "permissions": ["dialog:allow-open"]
}
